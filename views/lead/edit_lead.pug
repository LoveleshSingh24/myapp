extends ../layout

block content
  form(action = "/lead/"+lead.id+"/edit",method="POST")
    input(name="lead_email" type='email',placeholder="Enter the email address",value = lead.email)
    //- exitising value in database
    button(type="submit") Save
